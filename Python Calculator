def calculator (a,b,OP):
    if OP=='+':
      print(a,'+',b,'=',a+b)
    elif OP=='-':
      print(a,'-',b,'=',a-b)
    elif OP=='*':
      print(a,'*',b,'=',a*b)
    elif OP=='/':
      print(a,'/',b,'=',a/b)
    else:
      print('Invalid Input') 

calculator(23,5,'*')
